meta {
  name: Crear Cliente 3
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/clientes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "nombreCliente": "Cliente Bodega 2",
    "estado": "ACTIVO"
  }
}

settings {
  encodeUrl: true
}
