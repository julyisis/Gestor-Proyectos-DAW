meta {
  name: Usuario Admin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/usuarios
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "nombreUsuario": "nuevo_admin",
    "password": "123",
    "estado": "ACTIVO",
    "rol": "admin"
  }
}

settings {
  encodeUrl: true
}
