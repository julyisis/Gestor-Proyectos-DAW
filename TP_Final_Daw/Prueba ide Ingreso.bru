meta {
  name: Prueba ide Ingreso
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "nombreUsuario": "juli_admin",
    "password": "123"
  }
}

script:post-response {
  if (res.body.access_token) {
    bru.setEnvVar("token", res.body.access_token);
  }
}

settings {
  encodeUrl: true
}
